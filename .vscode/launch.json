{
    "version":"0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "debugpy",
            "request": "launch",
            "module": "gui",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}